service: docker

addons:
    apt:
        packages:
            - curl

before_install:
    - docker run --rm -it -v $PWD:/workdir paperist/alpine-texlive-ja


script:
    - make clean
    - make all
